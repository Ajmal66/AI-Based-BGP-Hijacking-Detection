# Processed Datasets

This directory contains processed datasets generated by the project's
data preprocessing and feature engineering pipelines.

Large processed datasets are excluded from the GitHub repository.
They can be regenerated by running the corresponding preprocessing
and feature engineering scripts.
